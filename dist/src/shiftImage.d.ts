import { Canvas, Image } from "canvas";
/**
 * Shifts the image across multiple tiles for dat specification.
 */
export default function shiftImage(image: Image, inputSize?: number, outputSize?: number): Canvas;
